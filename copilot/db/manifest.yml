name: db
type: Backend Service

image:
  build: ./db/dockerfile
  port: 3306

cpu: 256
memory: 512
count: 1
exec: true

variables:
  MYSQL_USER: local
  MYSQL_PORT: 3306
  MYSQL_PASSWORD: local123
  MYSQL_ROOT_PASSWORD: mncinnocent
  MYSQL_DATABASE: face_reco

environments:
  development:
    count:
      spot: 1
  production:
    count:
      range:
        min: 1
        max: 5
      cpu_percentage: 70
      memory_percentage: 80
      requests: 100